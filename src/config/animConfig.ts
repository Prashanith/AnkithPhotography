const fadeInConfig = {
  hidden: {
    opacity: 0,
  },
  visible: {
    opacity: 1,
  },
};

const ANIM_DURATION = 1;

const DELAY = 0;

const ANIM_TYPE = "tween";

export { ANIM_DURATION, DELAY, ANIM_TYPE };
