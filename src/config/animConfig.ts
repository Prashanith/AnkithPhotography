const fadeInConfig = {
  hidden: {
    opacity: 0,
  },
  visible: {
    opacity: 1,
  },
};

const ANIM_DURATION =0.5;

const DELAY = 0;

const ANIM_TYPE = "spring";

export { ANIM_DURATION, DELAY, ANIM_TYPE, fadeInConfig };
